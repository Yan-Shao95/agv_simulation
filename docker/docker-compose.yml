services:
  agv-sim:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    network_mode: host
    environment:
      DISPLAY: ${DISPLAY:-host.docker.internal:0.0}
      QT_X11_NO_MITSHM: "1"
    volumes:
      - ..:/workspace
    stdin_open: true
    tty: true
